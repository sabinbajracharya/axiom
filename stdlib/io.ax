// std/io.ax — safe I/O API
//
// Extern "C" fn declarations. These are the platform boundary — once the
// CLI pipeline loads stdlib through the module system, these will move to
// core/platform.ax and this file will import from there.

pub extern "C" fn print(s: String);
pub extern "C" fn println(s: String);
