// Structs, enums, and exhaustive `match` — the headline v0 type-system subset.
// Type-checked at M2: struct literals, field access, and match exhaustiveness.
struct Point {
    x: Float,
    y: Float,
}

fn main() {
    val origin = Point { x: 0.0, y: 0.0 }
    val x_coord = origin.x
}