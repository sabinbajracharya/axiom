[package]
name = "desugar"
version = "0.0.0"
edition = "2021"
license = "MIT"
publish = false

[lints]
workspace = true

[dependencies]
resolver = { path = "../resolver" }
typecheck = { path = "../typecheck" }
lexer = { path = "../lexer" }

[dev-dependencies]
parser = { path = "../parser" }
