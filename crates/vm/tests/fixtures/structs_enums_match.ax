enum Shape {
    Circle(Float),
    Rect(Float, Float),
    Empty,
}

fn area(let s: Shape) -> Float {
    match s {
        Circle(r) => r * r,
        Rect(w, h) => w * h,
        Empty => 0.0,
    }
}

fn main() {
    val c = Circle(3.0)
    val a = area(c)
    print(string::format("{}", a))
}