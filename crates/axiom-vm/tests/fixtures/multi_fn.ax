fn square(n: Int) -> Int {
    n * n
}

fn sum_of_squares(a: Int, b: Int) -> Int {
    square(a) + square(b)
}

fn main() {
    val result = sum_of_squares(3, 4)
    println(string::format("{}", result))
}
