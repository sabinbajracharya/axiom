enum Shape {
    Circle(Int),
    Rect(Int, Int),
    Empty,
}

fn main() {
    let s = Shape::Circle(5)
    match s {
        Shape::Circle(r) => println(r),
        Shape::Rect(w, h) => println(w),
        Shape::Empty => println(0),
    }
}
