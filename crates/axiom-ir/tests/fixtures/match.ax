enum Shape {
    Circle(Float),
    Rect(Float, Float),
    Empty,
}

fn describe(let s: Shape) -> Float {
    match s {
        Circle(r) => r * r,
        Rect(w, h) => w * h,
        _ => 0.0,
    }
}
